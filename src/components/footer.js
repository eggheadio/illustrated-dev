import React from 'react'
import { css } from '@emotion/core'

const Footer = () => (
  <footer
    css={css({
      padding: '30px',
      textAlign: 'center',
    })}>
    © Maggie Appleton
  </footer>
)
export default Footer
