import styled from '@emotion/styled'
import { bpMinSM } from '../../utils/breakpoints'

export default styled.hr`
  text-align: center;
  max-width: 200px;
  margin: 60px auto;
  background: #eec57c;
`
